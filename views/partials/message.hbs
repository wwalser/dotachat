{{#if message}}
    {{#with message}}
    <div class="aui-message aui-message-{{type}} closeable">
        <p class="title">
            <strong>{{title}}</strong>
        </p>
        <p>{{message}}</p>
    </div>
    {{/with}}
{{/if}}